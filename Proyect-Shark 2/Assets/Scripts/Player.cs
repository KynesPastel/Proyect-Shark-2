using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{  
  
    [SerializeField] protected PlayerData playerStats;
    void Start()
    {   
    }

    // Update is called once per frame
    void Update(){}
     //if player moves the life goes down
     
        //if player has life then move
      /* if (playerStats.lifePlayer > 0)
        {
            //add 1 unit to z axis to current player (player moves)
           transform.position += new Vector3 (0,0,1) *playerStats.speedPlayer;
           playerStats.lifePlayer = playerStats.lifePlayer - 2;
           Debug.Log("Me entre la vida y la muerte :O"); 

        }*/
       
    

       
       
       

          


       
        
        
           

} 
